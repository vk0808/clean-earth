import React from "react";
import { Formik } from "formik";

const Form = () => {
  return (
    <section className="pledge-form">
      <div className="form"></div>
    </section>
  );
};

export default Form;
